# Pinned for reproducible Render / Railway / GitHub Actions installs.
# One file covers the operations desk and the scrape -> match -> outreach cycle.

# Operations desk
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Supabase Data API + optional schema apply
supabase==2.31.0
python-dotenv==1.2.3
psycopg[binary]==3.3.5

# Scraper + enrichment + outreach
apify-client==3.2.0
openai==3.13.0
anthropic==1.5.0
httpx==0.28.1
rich==15.0.0
bcrypt==4.3.0
stripe==13.2.0
